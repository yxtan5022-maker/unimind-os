# UniMind 路线图

[English](ROADMAP.md) | 简体中文

UniMind 是一个用户空间量子-经典中间件研究原型。本路线图跟踪真实的、当前范围明确的工程工作。它刻意排除虚构功能（没有"AI 原生操作系统"、没有虚拟内存扩展、没有内核绕过）。

## 阶段 1：核心对齐（当前）

* **Unibit 正确性：** 滑动窗口频率（式 2）、sinc 折叠（式 3）与阈值折叠（式 4）在 Python 与 Rust FFI 引擎中实现完全一致；由 42 个自动化 pytest 测试与 Python-vs-Rust 一致性校验验证。
* **数学验证：** 使用固定种子的 Qiskit AerSimulator 实验显示经验测量频率收敛于理论权重（`|<Z>_经验 - <Z>_理论| = 0.0110 < 0.05`）。
* **性能基准：** NumPy 加速比 7.0x-10.5x；Rust FFI 端到端加速比相对纯 Python 为 3.0x-3.3x。
* **诚实的沙箱：** 受限命名空间执行器，通过静态分析拒绝风险模式；基于规则的回退；对不支持的意图动作返回诚实的"不支持"响应。
* **硬件探针：** `kernel/topo_mapper.cpp` 输出 JSON 主机检测信息（核心数、OS、架构、内存）。

## 阶段 2：稳健性与覆盖

* **后端广度：** 系统化测试量子后端矩阵（qiskit classical/aer/ibm、cudaq），以及后端缺失时的优雅降级。
* **编排器可靠性：** 改进 LLM 计划验证与基于规则的回退；如实记录失败模式。
* **跨平台打包：** 验证 Windows/macOS/Linux 上的 Rust FFI 动态库与启动脚本构建。

## 阶段 3：研究方向

* **编码研究：** 评估不同平滑窗口与折叠阈值对下游编码质量的影响。
* **噪声敏感性：** 测量测量噪声如何在真实（非理想）后端上影响恢复频率。
* **集成：** 将硬件探针与精度选择端到端接入编排路径。
